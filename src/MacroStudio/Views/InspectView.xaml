<UserControl x:Class="MacroStudio.Views.InspectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MacroStudio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	
	<StackPanel>
		<StackPanel Orientation="Horizontal">
			<Label Content="Current cursor position:" Margin="0,5" />
			<Label Content="{Binding CursorPosition}" Margin="5" />
		</StackPanel>

		<StackPanel Orientation="Horizontal">
			<Label Content="X: " Margin="0,10" />
			<TextBox Text="{Binding XPos}" MinWidth="50" Margin="5" Padding="5" />
			<Label Content="Y: " Margin="0,10" />
			<TextBox Text="{Binding YPos}" MinWidth="50" Margin="5" Padding="5" />
			<Button Content="Set cursor position" Margin="5" Padding="10,5" Command="{Binding SetCursorPositionCommand}" />
		</StackPanel>
	</StackPanel>
	
</UserControl>
