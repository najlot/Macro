<UserControl x:Class="MacroStudio.Views.ExecuteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:AvalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
			 xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:local="clr-namespace:MacroStudio.Views"
			 xmlns:behaviors="clr-namespace:MacroStudio.Behaviors"
			 mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<DockPanel>
		<Grid DockPanel.Dock="Bottom">
			<WrapPanel HorizontalAlignment="Left">
				<Button Command="{Binding SaveCommand}" Content="Save" Margin="5" Padding="10,5" />
				<Button Command="{Binding LoadCommand}" Content="Load" Margin="5" Padding="10,5" />
			</WrapPanel>
			<WrapPanel HorizontalAlignment="Right">
				<Label Content="Executions: " Margin="0,10" />
				<TextBox Text="{Binding Executions}" Margin="5" />
				<Button Command="{Binding RunCommand}" Content="Run" Margin="5" Padding="10,5" IsEnabled="{Binding IsRunButtonEnabled}" />
			</WrapPanel>
		</Grid>

		<AvalonEdit:TextEditor DockPanel.Dock="Top" Name="Code" ShowLineNumbers="True" SyntaxHighlighting="C#" >
			<i:Interaction.Behaviors>
				<behaviors:TextEditorBehavior BindedText="{Binding Code, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
				<behaviors:TextEditorCompletionBehavior />
			</i:Interaction.Behaviors>
		</AvalonEdit:TextEditor>
	</DockPanel>
</UserControl>
