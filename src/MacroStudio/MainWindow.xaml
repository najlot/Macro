<controls:AdonisWindow x:Class="MacroStudio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:controls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:local="clr-namespace:MacroStudio"
		xmlns:views="clr-namespace:MacroStudio.Views"
		mc:Ignorable="d"
        Title="Macro Studio"
		Height="450" Width="800">

	<Window.Style>
		<Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
	</Window.Style>

	<Grid>
		<TabControl SelectionChanged="TabSelectionChanged" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}" >
			<TabItem DataContext="{Binding RecordViewModel}">
				<TabItem.Header>
					<TextBlock Text="Record" HorizontalAlignment="Center" MinWidth="100" />
				</TabItem.Header>
				<views:RecordView DataContext="{Binding .}"/>
			</TabItem>
			<TabItem DataContext="{Binding ExecuteViewModel}">
				<TabItem.Header>
					<TextBlock Text="Execute" HorizontalAlignment="Center" MinWidth="100" />
				</TabItem.Header>
				<views:ExecuteView DataContext="{Binding .}"/>
			</TabItem>
			<TabItem DataContext="{Binding InspectViewModel}">
				<TabItem.Header>
					<TextBlock Text="Inspect" HorizontalAlignment="Center" MinWidth="100" />
				</TabItem.Header>
				<views:InspectView DataContext="{Binding .}"/>
			</TabItem>
			<TabItem DataContext="{Binding GlobalMethodsViewModel}">
				<TabItem.Header>
					<TextBlock Text="Built in functions" HorizontalAlignment="Center" MinWidth="100" />
				</TabItem.Header>
				<views:GlobalMethodsView DataContext="{Binding .}"/>
			</TabItem>
		</TabControl>
    </Grid>
</controls:AdonisWindow>
